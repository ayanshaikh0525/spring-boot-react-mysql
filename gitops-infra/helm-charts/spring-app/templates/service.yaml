apiVersion: v1
kind: Service
metadata:
  name: spring-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: spring-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
